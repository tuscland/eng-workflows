interface:
  display_name: "Implement Ticket"
  short_description: "Implement one ticket in an isolated worktree"
  default_prompt: "Use $implement-ticket to implement this ticket and open its draft pull request."
policy:
  allow_implicit_invocation: false
