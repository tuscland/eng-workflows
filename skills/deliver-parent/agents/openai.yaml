interface:
  display_name: "Deliver Parent"
  short_description: "Implement and integrate a parent locally"
  default_prompt: "Use $deliver-parent to audit this planned parent, implement multiple tickets on unsigned local branches or a sole ticket directly on the integration branch, integrate cleared work locally, and finish validation without SSH or GitHub publication."
